The project is a daisy seed esp32 process -control combo microcontroller setup. 
Currently the communication between the esp32 and the daisy seed  works however when too much effects such as teh overdrive thorugh the endpoint api through my interface or even through only the endpoint the protocol breaks down
When i then use the Daisy/Utils/SerialPCInterface i am still able to communicate with the daisy seed however. I have a feeling that it is something end point specific. I.e in the interaction between the daisy seed and the esp32

Findings:
- It mainly happens after adding too much distortion effect, even after adding too much ligher effects it happens but more rarely
- Usb serial doesnt break down. Only the end point ocmmand responses
- A clean restart of the daisy seed resolves the problem without the esp32 needing to be reset however the underlyuing issue persists

What i want from you:
- Keep the current communciation protocol with the same commands on the daisy seed side
- change what you need to the actual uart/serial communication where needed to make daisy seed work more relaibley with the esp32 and be clean about it not too convoluted code


Technical details:

    - The daisy seed and esp32 are connected as follows D16(ESP32) -> D13/UART1Tx (Daisy), D17(ESP32) -> D14/UART1Tx (Daisy) , GND(ESP32) -> AGND(Daisy)
    - Esp 32 can be built and uploaded through platformio
    - Daisy seed can be put in DFU mode (needed for upload) through the firmware/daisy/Utils/enter_dfu.py and firmware is uploaded by running the build_and_program_dfu cmake task
    - Find a way to reset the daisy seed completely through software as to reset the serial connection, this helps in debugging
    - Try to recreate the issue first by for example adding a few distortions or overdrives through the api end point and you can then see if using the usb serial would still work. This would narrow it down to the esp32 and daisy seed protocol
    - Use pio run -t upload to upload to the esp32 (however i suspect its a daisy seed issue since a reset of the daisy seed resets the problem completely without touching the esp32)
    - You have the autonomy to upload to these devices and test wheter your implementations work
    - The communication protocol works for now however breaks down in the  provided subcases
