# Firmware

This directory contains the embedded C/C++ projects for the microcontrollers used in the pedal.

## Structure
- **`audio-dsp/`**: The Daisy Seed firmware responsible for high-speed audio processing.
- **`control-mcu/`**: The ESP32 firmware responsible for routing UI inputs, managing hardware buttons, and hosting the web interface.
